spring:
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://example.com/.well-known/jwks.json
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
